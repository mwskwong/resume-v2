import { Name } from "@/types";

export const firstName = "Matthew";
export const middleName = "Wang Shun";
export const lastName = "Kwong";
const name: Name = { firstName, middleName, lastName };

export default name;